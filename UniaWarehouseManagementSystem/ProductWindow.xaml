<Window x:Class="UniaWarehouseManagementSystem.ProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UniaWarehouseManagementSystem.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="400" Width="400" ResizeMode="NoResize">

    <Window.DataContext>
        <vm:ProductEditorViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Kod Produktu (Unikalny):" FontWeight="Bold"/>
            <TextBox Text="{Binding Code}" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Nazwa Produktu:" FontWeight="Bold"/>
            <TextBox Text="{Binding Name}" Padding="5"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Jednostka:" FontWeight="Bold"/>
                <ComboBox ItemsSource="{Binding AvailableUnits}" SelectedItem="{Binding Unit}" Padding="5"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Stan Minimalny (Alarm):" FontWeight="Bold"/>
                <TextBox Text="{Binding MinStock}" Padding="5"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Opis / Uwagi:" FontWeight="Bold"/>
            <TextBox Text="{Binding Description}" AcceptsReturn="True" TextWrapping="Wrap" Height="80" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <Button Grid.Row="4" Content="ZAPISZ PRODUKT" Command="{Binding SaveCommand}" 
                Height="40" Background="#2196F3" Foreground="White" FontWeight="Bold" FontSize="14"/>
    </Grid>
</Window>