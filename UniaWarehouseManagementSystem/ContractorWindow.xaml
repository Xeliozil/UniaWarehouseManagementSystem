<Window x:Class="UniaWarehouseManagementSystem.ContractorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UniaWarehouseManagementSystem.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="350" Width="400" ResizeMode="NoResize">

    <Window.DataContext>
        <vm:ContractorEditorViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Nazwa Firmy / Imię i Nazwisko:" FontWeight="Bold"/>
            <TextBox Text="{Binding Name}" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="NIP (opcjonalnie):" FontWeight="Bold"/>
            <TextBox Text="{Binding NIP}" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Adres:" FontWeight="Bold"/>
            <TextBox Text="{Binding Address}" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,10,0,20">
            <TextBlock Text="Rola (zaznacz):" FontWeight="Bold" Margin="0,0,0,5"/>
            <StackPanel Orientation="Horizontal">
                <CheckBox Content="Dostawca" IsChecked="{Binding IsSupplier}" Margin="0,0,20,0"/>
                <CheckBox Content="Odbiorca (Klient)" IsChecked="{Binding IsRecipient}"/>
            </StackPanel>
        </StackPanel>

        <Button Grid.Row="4" Content="ZAPISZ" Command="{Binding SaveCommand}" 
                Height="40" Background="#FF9800" Foreground="White" FontWeight="Bold"/>
    </Grid>
</Window>